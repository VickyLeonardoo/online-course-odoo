<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="online_course.list">
      <field name="name">online_course list</field>
      <field name="model">online_course.online_course</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="online_course.action_window">
      <field name="name">online_course window</field>
      <field name="res_model">online_course.online_course</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="online_course.action_server">
      <field name="name">online_course server</field>
      <field name="model_id" ref="model_online_course_online_course"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="online_course" id="online_course.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="online_course.menu_1" parent="online_course.menu_root"/>
    <menuitem name="Menu 2" id="online_course.menu_2" parent="online_course.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="online_course.menu_1_list" parent="online_course.menu_1"
              action="online_course.action_window"/>
    <menuitem name="Server to list" id="online_course" parent="online_course.menu_2"
              action="online_course.action_server"/>
    -->
  </data>
</odoo>